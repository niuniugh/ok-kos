# Database (used by docker-compose for both PostgreSQL container and app)
POSTGRES_USER=okkos
POSTGRES_PASSWORD=change-me-to-a-strong-password
POSTGRES_DB=okkos

# App
SESSION_SECRET=change-me-generate-with-openssl-rand-hex-32
